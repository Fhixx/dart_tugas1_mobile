name: mobile_kalku
description: "NusaFit — Aplikasi kesehatan dan kalender Nusantara."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.9.0
  pointycastle: ^3.5.0
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.3.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.2.0

flutter:
  uses-material-design: true
